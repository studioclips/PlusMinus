/*
 * カメラの距離として
 * ６ｘ６のときは距離を１２に、５ｘ５のときは１０に、４ｘ４のときは８に、３ｘ３のときは６に設定
 * */
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StageUseCase : MonoBehaviour
{
    [SerializeField]
    private Camera 
    // Start is called before the first frame update
    void Start()
    {
        //  4x4として一旦設定
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
